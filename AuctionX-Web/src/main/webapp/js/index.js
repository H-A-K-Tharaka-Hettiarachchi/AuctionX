// This is the external JavaScript file for your welcome page.
// The background animations are primarily handled by CSS.

document.addEventListener('DOMContentLoaded', function() {
    // --- Add JavaScript logic for the welcome page here ---
    // This page might be mostly static, but you could add:
    // e.g., Animations on scroll
    // e.g., Dynamic content loading
    // e.g., Interactive elements if you add any to the welcome page

    console.log('index.js loaded');
});